"""Review pipeline package — stub for WP1 overrides only.

The full review_pipeline package (7 modules: contribution_rows, normalizers,
table_builder, figure_data, audit, overrides, export) will be built during
WP2-WP6 per d-012 (incremental extraction from existing modules).

Currently only the overrides sub-module is implemented (WP1 T2.1).
"""
