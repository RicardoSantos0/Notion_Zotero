.venv/
__pycache__/
*.pyc
*.pyo
.env
notion_zotero.config.json
.notion-zotero.json
notion-zotero.json
data/
data/pulled/
logs/
# Excel outputs are regenerable binaries — keep local, never upload (CSV/MD are the tracked forms)
*.xlsx
# Frozen baseline snapshot is a local reference — do not upload its binary assets
baseline_manual/*.xlsx
baseline_manual/*.docx
.idea/
.vscode/
# ignore fixtures directory (keep local only)
/fixtures/
# keep ignoring the merged canonical fixture file explicitly
fixtures/canonical_merged.json
# Migration audit raw report (may contain private content)
migration_audit/migration_report.json
.pytest_cache/
*.egg-info/
dist/
.coverage
.claude/settings.local.json
.claude/settings.json
tests/fixtures/la_table_3_counts.csv
