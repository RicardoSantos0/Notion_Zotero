# Reading List → Canonical mapping

Mapping rules from reading-list fixture structures (tables, blocks, properties)
to canonical JSON fields. This file should contain examples mapping a sample
table into a `task_extraction` and a `reference_task`.

Examples and edge cases will be added incrementally.
