[pytest]
python_files = test_*.py
testpaths = tests
addopts = --cov=notion_zotero --cov-report=term-missing --cov-fail-under=80 --cov-config=.coveragerc
markers =
    slow: tests that make real network calls or take >5s (deselect with -m "not slow")
